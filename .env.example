# StockDesk environment variables
# The app runs fully offline (IndexedDB) with NO env vars set.
# Add these to enable cloud sync + auth via Supabase.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
